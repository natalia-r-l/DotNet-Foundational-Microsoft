/*

// As variváveis que usam a palavra var devem obrigatoriamente ser inicializadas, porque é por meio da inicialização
// que o compilador consegue interpretar a variável, e atribuir um tipo a ela. 
// Caso a variável var seja chamada, sem ter sido inicializada, ocorrerá um erro. 
// Caso após a inicialização, for passado um novo valor à variável var, que não seja do mesmo tipo que foi definido
// para ela via inicialização, também ocorrerá um erro. 

// Declaração da variável var, e inicialização da variável para que seja atribuída como tipo string. 
var message = "Hello World!";

// Redefinição do valor da variável para outro valor string.
message = "Natalia";

// Simulação de erro gerado ao tentar atribuir um valor a variável var, de tipo diferente ao que foi inicializado. 
//message = 10.703m;

Console.Write(message);

*/